# 署名存证（作品指纹）

这份文件回答一个问题：**日后怎么证明「雪鸮」就是这份作品的作者。**

只写一个署名文件是不够的——谁都可以写一个。真正管用的是
「**公开** + **带时间** + **可校验**」这三件事同时凑齐。

## 一、作品信息

| 项 | 内容 |
|---|---|
| 作品名称 | MindMap（单机思维导图 · AI 辅助思考工具） |
| 作者署名 | 雪鸮 |
| 首次完成 | 2026-09-12 |
| 版本 | 1.0.0 |
| 许可 | MIT（见项目根目录 `LICENSE`） |

## 二、作品指纹

指纹的算法：把全部源代码与配置文件逐个算 SHA-256，再把「哈希 + 文件名」
按文件名排序拼成一整块，对这块整体再算一次 SHA-256。
任何一处被改动——哪怕只改一个字符、只删一个文件——指纹都会变。

- 逐文件清单：`docs/provenance.sha256`
- 当前总指纹：`3c33314f6cf1f805fb413b9ca67948e253a31060236e790c1f7f79ba3d629553`
- 生成方式：`powershell -ExecutionPolicy Bypass -File tools/make_provenance.ps1`

最近一次生成（对应 v1.0.0 的发布版本）

```
生成时间：2026-09-13 00:17
文件数：  124
总指纹：  3c33314f6cf1f805fb413b9ca67948e253a31060236e790c1f7f79ba3d629553
```

> 修改过 `AUTHORS.md` 的"身份绑定"之后，指纹会随之更新一次；每次改动都要重新生成。

> 发布时把这段原样贴到你能控制的公开渠道（仓库 README、社交账号、个人主页均可），
> 这行指纹就是"当时那份代码"的凭证。

> 指纹会随代码改动而变。**发布某个版本时重新生成一次**，
> 把当时的指纹连同版本号一起对外公布；之后再改动代码，不影响那一次公布过的记录。

## 三、让证据站得住（成本从低到高）

1. **公开仓库**（免费，最推荐）
   把项目推到 GitHub / Gitee 的公开仓库。平台的提交历史带服务器时间，
   能证明"某个时间点你就在维护这份代码"，而仓库所有者正是你的账号——
   这一步就把笔名「雪鸮」和你本人绑在了一起。
2. **在你能控制的渠道发一条同款声明**（免费）
   用你的邮箱、社交账号或主页发布"MindMap 由我（雪鸮）制作"，
   并写上当时的作品指纹。平台自带的时间就是可信的"时间"。
3. **第三方可信时间戳**（几元到几十元）
   把指纹文件提交给时间戳服务机构（如联合信任 TSA、中国版权保护中心 DCI），
   换回一份带权威时间的存证证书。
4. **软件著作权登记**（约几百元，需实名）
   国内效力最强的一种。需要真实姓名与身份证件——
   也就是说，登记材料本身就把"雪鸮"和你的法定身份对应起来了。

## 四、别人换皮时，你能主张什么

- 作品以 MIT 许可发布，别人**可以**使用、修改、闭源、商用——这是许可授权的范围。
- 但 MIT 明文要求**保留版权声明**。对方可以加上自己的名字，不能把你的删掉。
- 若对方删除 `Copyright (c) 2026 雪鸮` 并宣称全部由他创作，即违反授权条件，构成侵权。
  此时你的举证材料是：`AUTHORS.md` 署名声明 + 公开仓库的提交历史 /
  公开渠道那条带时间的声明 + 能对得上的作品指纹。
- 你本人始终保留著作权，随时可以把自己的代码嵌进付费软件——MIT 是非独占授权，
  你把代码交出去的同时并没有把自己的使用权交出去。

## 五、怎么校验指纹

```powershell
# 重新生成一份清单，与发布时公布的那一份逐行对比
powershell -ExecutionPolicy Bypass -File tools/make_provenance.ps1
```

两份清单完全一致 = 手上的代码就是当初发布的那一份，未被改动。
